# Clanker school development log

## 2026-09-09 — first executable foundation unit

The owner approved prerequisite-based schooling. Issue #133 implements a
real-runtime diagnostic, typed grading, parameter freezing with working
conversation memory, chained attempts, deterministic scheduling and tutor
cards. Skill readiness is conditional on named authored evidence, not a
universal capability claim. No automatic teacher/promotion service is running.

The first curriculum test caught duplicate appraisal exercises in two phase
labels. Context identity now binds actual input/expectation content; phase-
specific people make these exercises genuinely different. Shared generator
lineage remains explicit rather than pretending independent evidence.

A source/time exercise exposed 'Alex yesterday' being stored as one source.
The temporal candidate fixes that attachment before entity parsing. The
source-question test then found a second bug: WHO/from selected the patient.
The candidate explicitly binds source and preserves the known patient.
Grading now checks the requested role and selected value, not just whether
the full proposition happens to contain the source's name.

Per-phase local diagnostic, real V8, CPython 3.13.5:
- Main 29e2c5f...: 9/27 episodes; 60/108 checks.
- Main plus candidate: 15/27; 78/108.
- Separately delivered dc7871f... decoder: 15/27; 84/108.
- Delivered plus candidate: 21/27; 102/108.

47 focused tests pass. No production engine, runtime, or evaluator bytes are
modified. Remaining failures block relevant prerequisites. Complete transcript
and per-check reports are generated by the documented CLI. The current task
is a measured curriculum/repair experiment, not memorization of replacement
sentences or a finished general tutor.

Review the candidate exception/rollback limitations, conservative time scope,
parameter-freeze seams, and distinction between metadata integrity and truth.
See experiments/school/README.md for commands and evidence boundaries.
